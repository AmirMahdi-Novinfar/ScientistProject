<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ketaboon">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Ketaboon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ir.iamnovinfar.ketaboon.activity.ContentActivity"></activity>
        <activity android:name="ir.iamnovinfar.ketaboon.activity.LoginActivity" />
        <activity
            android:name="ir.iamnovinfar.ketaboon.activity.Splashactivity"
            android:theme="@style/AppThemeNoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ir.iamnovinfar.ketaboon.activity.MainActivity"
            android:label=""

         >

        </activity>
    </application>

</manifest>